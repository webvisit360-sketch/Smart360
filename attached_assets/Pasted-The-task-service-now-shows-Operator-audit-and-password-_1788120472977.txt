The task service now shows "Operator audit and password rotation" as the
active task. That is #54. It stays unstarted, whatever the card says, and it
must not begin. If it requires the owner's password, the answer is the same as
this morning: no.

Continue C1. Three things, in this order.

1 · REMOVE THE SILENT CATCH
    try { nearCandidates = await getCachedCreatorNearRing(...); }
    catch { /* strict global path remains available */ }

That bare catch swallowed an Overpass 504. The run continued with an empty
catalogue and reported nothing, and we spent the evening reasoning about a
matcher that never received a candidate.

You wrote the rule yourself and I adopted it: never convert infrastructure
failure into editorial rejection. The rule is in your report and its opposite
is in the code.

Record per run, durably and in the run report: HTTP status, duration, raw
element count, filtered count, and any error. The near ring must never
contribute nothing without the report saying whether the catalogue was empty
or the request failed. Apply this to every external dependency in the Creator.

2 · REBUILD THE QUERY — it 504s because it is too heavy
Nine `around:35000` searches, several on bare keys. `[amenity][name]` alone
asks for every named bench and car park within 35 km.

· bounding box instead of `around:`, then apply the circle locally;
· explicit value whitelists instead of bare keys — amenity limited to
  place_of_worship, monastery, museum, theatre, arts_centre; leisure to park,
  nature_reserve, garden; natural to peak, waterfall, cave_entrance, spring,
  water, cliff; man_made to tower, lighthouse; historic may stay broad;
· split into several smaller requests and cache the merged catalogue;
· raise the timeout and add one backoff retry as a safety net, not as the fix.

Report the new query, its status, duration and element count for the Menina
origin, and then answer the question run four could not: are Mozirski gaj and
Golte in the catalogue?

3 · THE PROMPT DOES NOT SAY WHERE IT IS
Derive the surrounding settlements machine-side — the origin's municipality
and its neighbours — put those names in the prompt, and require a concrete
minimum: at least eight proposals from that named set. That is not the model
supplying a fact; it is us telling it where it stands.

Then run five. No approvals. No #54.

Report and STOP.