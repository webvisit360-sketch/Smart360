I uploaded an updated ui/ folder. Read ui/README.md first and follow it literally. It now contains two themes plus a cover editor spec.

1. Two selectable themes. Add theme to Tenant with values mediterran and swipe, editable in the admin as a two-option picker per tenant. Load only one stylesheet per request: tema-sredozemska.css for mediterran, tema-poteg.css for swipe. Keep the existing Mediterranean screens exactly as they are — they already match zasloni-sredozemska.html.

2. New swipe theme. Build it to match ui/zasloni-poteg.html class for class. .app → .pager with horizontal scroll-snap and scroll-snap-stop: always, each .screen full width and 100dvh. The first screen is .cover: a full-bleed photo or the 3DVista <iframe>, an even .cover__veil over the whole image, logo and round buttons on top, .cover__txt with title, subtitle and rating line at the bottom, and the "Povlecite levo" hint. Then one screen per section (.sc with .grid2/.gc or .rowlist/.row), and a contact screen last. .dots at the bottom show position and are clickable, white on the cover (.on-dark). No left/right arrow buttons anywhere. No bottom tab bar in this theme. Category detail is the #detail overlay sliding in from the right with its own horizontal pager (.dpager/.dscreen) so a swipe moves between categories of the same section, with chips on top for direct jumps; fill only the current screen and its neighbours, not all at once.

3. Cover editor. Implement exactly ui/urejevalnik-naslovnice.md: add the cover* fields to Tenant with the stated ranges and defaults, build the admin editor with a live preview, and pass the values to the public page as the CSS variables listed in that file (server-side as a style attribute on .cover so there is no flash on load). It applies to both themes. A working example is the slider button at the top right of the cover in ui/predogled-poteg.html.

Do not write any new CSS, do not add component libraries, and do not change the Mediterranean theme while doing this.