Admin UX redesign, two views, per the two approved mockups — PLUS two-way binding with the finished host onboarding form. The visual rework changes no endpoints, guards, or data rules. Shared CGP values, EXACTLY: white page; cards #F4F6F2 with 1px #E8EBE6 border, radius 16px; inner row cards white, 1px #E8EBE6, radius 10px; accent #157347 (all primary buttons, icons, active chips — white text on green); secondary text #66716A; muted #9AA39D; dashed add-rows border #C9D2CB; Archivo. Icons: the app's existing icon set rendered as REAL icons — also FIX the current bug where icon KEYS render as literal text overlapping titles ("home", "waves", "bag", "compass").

VIEW 1 — "Struktura vsebine" (tenant content structure):
1. Each top section (Vaš kamp / Naša ponudba / Odkrijte okolico …) is one card: header row = green icon tile (34px, radius 10, white icon), section title 18px/800, quiet content count ("8 elementov" / "18 kategorij · 11 krajev"), "Uredi sekcijo" as green text-with-pencil on the right.
2. Items are COMPACT rows (one line, ~46px): small icon tile or chevron, item name 15.5px/600, "Skrito" pill where hidden (eye-off icon, #F4F6F2 bg), quiet Slovenian type label in muted color ("Besedilo", "Kartice", "Izdelek", "Apartmaji" — never "text/cards/products"), pencil at right. No large empty gaps anywhere.
3. Every add row is a dashed full-width row (+ icon, green 700 text) at the END of its group, labeled exactly "+ Dodaj …" — never floating in empty space.
4. In Odkrijte okolico: categories come from the CURRENT shared skeleton (post-alignment: Pohodništvo and Kolesarjenje separate, Zdravstvo, Dogodki included), in skeleton order; host-created custom categories appear after them with a quiet "gostiteljeva" marker. Categories WITH content show a small category header (icon tile + name + count) and their item rows; EMPTY categories collapse to a single compact row — icon tile, name, "· prazno" in muted, inline green "+ Dodaj …" on the right. (Admin shows ALL categories; guests see only those with visible content — existing rule, unchanged.)
5. Applies to all tenants; nothing is hidden or removed — same information, friendlier skin.

VIEW 2 — "Dodaj kraj v Okolico" dialog (replaces the current unfriendly add flow for okolica categories; house/offer add flows unchanged):
1. One dialog, three natural steps stacked: (a) field "IME KRAJA ALI DOŽIVETJA" with green "Poišči" button — name only, no address/coordinate fields; helper "Vpišite ime — kraj poiščemo na zemljevidu, razdaljo izračunamo sami."
2. (b) "ZADETKI — IZBERITE PRAVEGA": result rows with name (700), full address in muted below, and a pill on the right with the computed "km · min" (green pill on the selected row, gray on others); selected row 2px #157347 border + check icon. Below results: "Ni pravega zadetka? Postavite točko ročno na zemljevidu →" linking to the EXISTING manual-pin flow.
3. (c) "KATEGORIJA" as chips (selected green filled, others outlined), smart-preselected from the search result, "Vse kategorije ⌄" overflow for the rest.
4. Footer: note "Opis in fotografije dodate po vnosu — kot osnutek, gostje vidijo šele po objavi.", buttons "Prekliči" (outlined) and "Dodaj v vodnik" (green).
5. Behavior stays the existing editor-search pipeline unchanged: same search endpoint, duplicate guard ("že v vodniku"), OSM identity, OSRM distance, provenance, server-side rejection of plain okolica items. Purely a new skin and step order.

TWO-WAY BINDING (new functionality): the host onboarding form and the admin structure must be two views over the SAME tenant draft data — never two copies.
1. ADMIN → FORM: whenever the form opens (first onboarding or reopened later), every field is prefilled from the CURRENT admin draft content — texts, photos, videos, offers with prices, contacts, Wi-Fi, house rules, events — identically and completely. Data the operator entered in the admin appears in the form exactly.
2. FORM → ADMIN: everything the host saves or submits in the form lands identically in the tenant's admin draft structure — same fields, same items, media included. Okolica name rows keep going through the existing Creator queue (name hint, host provenance); everything else binds directly to the draft fields/items.
3. Remove/merge any separate onboarding copy so no divergence is possible; if the current suggestion-on-conflict mechanism from the first onboarding contradicts direct binding, say how you resolved it in the report. Guest visibility remains governed solely by the operator's publish (Zakon 1 snapshot) — the binding never publishes anything.
4. The form's sections must match its final shape: naziv nastanitve, naslov, telefon, e-pošta, spletna stran, prijava/odjava, kontaktne osebe (multiple), Wi-Fi, hišni red, ponudba (naziv+cena), okolica per skeleton category incl. Dogodki (naziv+datum+ura) and Svoja kategorija, fotografije.

All strings Slovenian. Verify in development with a temporary development session: structure view renders all sections per spec with real icons and no giant gaps (include a screenshot); an empty category renders as one compact row; the add dialog completes a real search→select→category→add flow and the duplicate guard still fires; operator-entered text+photo appear prefilled in the reopened form, and a form edit appears in the admin structure; house/offer add flows unchanged; nothing reaches the guest snapshot without publish. Report and STOP. No publication.

End your reply with a section titled "POVZETEK ZA LASTNIKA", written in Slovenian, as plain text I can copy.