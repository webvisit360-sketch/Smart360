Four corrections to the host onboarding form, from the owner's review.

1. CATEGORIES MIRROR THE SKELETON 1:1 — never merge or rename. "Pohodništvo" and "Kolesarjenje" are SEPARATE categories (as at Meli Pu) and must appear as separate blocks with exactly those names. If the form currently shows a merged "Pohodi in kolesarjenje", that is a bug in the form's category source — fix it so the form always renders the shared skeleton's okolica categories exactly, one block per category, in skeleton order. If any tenant-level section actually merges them in the skeleton itself, report that separately — do not paper over it in the form.
2. ADD BUTTON LABEL: every add row in the form says exactly "+ Dodaj …" — not "Dodaj kraj", not "Dodaj element" — everywhere in the form.
3. CUSTOM CATEGORY: at the end of section 5 add "Svoja kategorija" — the host can type a category name of their own (e.g. something none of the offered categories covers) and add name-only entries under it, multiple custom categories allowed. On submit these arrive to the operator clearly marked as host-created categories with their entries; they are created as per-tenant additional okolica categories through the existing category tooling (no ad-hoc mechanism), subject to the same operator review as everything else.
4. GUEST VISIBILITY RULE (owner decision, applies to the guest guide, not just the form): a category with ZERO visible items is NOT shown to guests (no chip, no empty section); as soon as it has at least one visible item, it appears automatically. The admin always shows ALL categories regardless of content. Apply this in the guest nav/chips resolver if it does not already behave this way, without touching per-tenant isVisible deactivation, which stays as a separate operator control.

Verify in development: the form lists Pohodništvo and Kolesarjenje as separate blocks with skeleton order; all add rows read "+ Dodaj …"; a custom category with two entries survives autosave and arrives marked as host-created on submit; a category with no visible items shows no chip and no section in the guest guide, and appears automatically after its first visible item; admin still shows all categories. Report and STOP. No publication.

End your reply with a section titled "POVZETEK ZA LASTNIKA", written in Slovenian, as plain text I can copy.